{
  "devDependencies": {
    "@vitest/browser-playwright": "^4.0.18",
    "vitest": "^4.0.18"
  },
  "scripts": {
    "test": "vitest",
    "test:browser": "vitest --config=vitest.browser.config.js"
  }
}
